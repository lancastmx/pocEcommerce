import { EstiloDivertidoPipe } from './estilo-divertido.pipe';

describe('EstiloDivertidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstiloDivertidoPipe();
    expect(pipe).toBeTruthy();
  });
});
